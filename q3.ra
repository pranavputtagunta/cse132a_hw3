(
    \rename_{name} \project_{cname} (
        borrower \join_{lno = no} (\select_{type = 'jumbo mortgage'} loan)
    )
)
\intersect
(
    \rename_{name} \project_{cname} (
        borrower \join_{lno = no} (\select_{type = 'student'} loan)
    )
);